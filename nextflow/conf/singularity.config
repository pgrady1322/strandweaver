/*
========================================================================================
    Singularity Configuration
========================================================================================
*/

singularity {
    enabled = true
    autoMounts = true
    runOptions = '--nv'  // Enable NVIDIA GPU support
}

process {
    container = 'docker://strandweaver/strandweaver:0.1.0'
}
