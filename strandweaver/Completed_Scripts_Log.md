A log of scripts in their finalized form, so that they are not consolidated or modified into a different one.

Rules in general: "module" in the name of a script or program specifies that it is a major program component. "utility" similarly specifies an independent helper or separate heuristic backup to an AI/ML module.

Script components in a finalized form:

