# ═══════════════════════════════════════════════════════════════════════
# StrandWeaver .gitignore
# ═══════════════════════════════════════════════════════════════════════
# Training data and tarballs stay LOCAL ONLY — never pushed to GitHub.
# Trained model weights ARE tracked (small, needed for releases).


# ── Training data (large — LOCAL ONLY) ────────────────────────────────
training_data/
training_data_10x/
training_data_ul/
training_data_*/
test_graph_only/
graph_csvs*.tar.gz
training_10x.log
training_*.log

# ── Python artifacts ──────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
*.egg
.pytest_cache/

# ── Environment ───────────────────────────────────────────────────────
venv*/
.env
*.env

# ── IDE / editor ──────────────────────────────────────────────────────
.vscode/*
!.vscode/settings.json
.idea/
*.swp
*.swo
*~
.vscode

# ── OS files ──────────────────────────────────────────────────────────
.DS_Store
Thumbs.db

# ── Nextflow work dirs ────────────────────────────────────────────────
.nextflow*
work/
nextflow/.nextflow*

# ── Internal dev files (not for GitHub) ────────────────────────────────
Non_Main_Commit_Files/
Non_Main_Commit_Files/TRAINING.md

# ── Misc ──────────────────────────────────────────────────────────────
archive/
models/
test_output/
test_archive/
pipeline_run.log
*.log
# Ignore large data files in trained_models but track model PKLs and metadata
trained_models/**/*.fastq
trained_models/**/*.fastq.gz
trained_models/**/*.fastq.gz.*
trained_models/**/*.fa
trained_models/**/*.fa.gz
trained_models/**/*.fai
trained_models/**/*.fasta
trained_models/**/*.bam
trained_models/**/*.bam.bai
trained_models/**/*.csv
trained_models/**/*.log
trained_models/nohup_alignment.out
trained_models/run_alignment.sh
